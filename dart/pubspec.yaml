name: dart_cpp_bridge
description: Dart side of a FRB-like C++ bridge (Phase 1 hand-written bindings).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  ffi: ^2.1.3

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
