name: mutex_repro
description: >
  最小复现：对比 @Native (hook) 和 DynamicLibrary.open 调用含 std::mutex 的
  C++ 函数时的行为差异。
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  code_assets: ^1.2.1
  dart_cpp_bridge:
    path: ../dart
  ffi: ^2.2.0
  hooks: ^2.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
