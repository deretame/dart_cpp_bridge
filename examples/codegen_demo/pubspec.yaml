name: codegen_demo
description: Fixture for dart_cpp_bridge codegen (SYNC/ASYNC/NORMAL).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  dart_cpp_bridge:
    path: ../../dart
  ffi: ^2.1.3

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
