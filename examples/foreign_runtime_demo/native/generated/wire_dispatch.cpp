// GENERATED by dart_cpp_bridge codegen — do not edit.
// NOTE (stdexec migration): this file was hand-ported to the std::exec model
// (exec::task + starts_on) while the codegen toolchain still targets
// async-simple; re-running codegen would regenerate the old Lazy form.
// Coroutine lambdas are avoided on MSVC (19.51 coroutine lambda capture bug,
// see src/cbridge.cpp): each method uses a static coroutine function.
#include "wire_dispatch.hpp"

#include "dart_cpp_bridge/codec.hpp"
#include "dart_cpp_bridge/dart_fn.hpp"
#include "dart_cpp_bridge/dispatch.hpp"
#include "dart_cpp_bridge/error_config.hpp"
#include "dart_cpp_bridge/object_handle.hpp"
#include "dart_cpp_bridge/runtime.hpp"
#include "dart_cpp_bridge/session.hpp"
#include "dart_cpp_bridge/stream_sink.hpp"

#include "api/foreign_api.h"

#include <stdexec/execution.hpp>
#include <exec/start_detached.hpp>
#include <exec/task.hpp>

#include <asio/post.hpp>

#include <chrono>
#include <cstdio>
#include <exception>
#include <memory>
#include <mutex>
#include <string>
#include <unordered_map>
#include <utility>
#include <vector>

namespace dcb {
namespace demo {

namespace {

void post_ok(const std::shared_ptr<Session> &s, std::uint64_t gen,
             std::uint64_t req, std::uint32_t method,
             const std::vector<std::uint8_t> &payload) {
  s->try_post(gen, make_frame(MsgType::kResponseOk, req, method, payload));
}

void post_err(const std::shared_ptr<Session> &s, std::uint64_t gen,
              std::uint64_t req, std::uint32_t method, const char *fn,
              const std::string &msg) {
  ByteWriter w;
  w.i32(1);
  w.str(dcb::error::format(fn, msg));
  s->try_post(gen, make_frame(MsgType::kResponseErr, req, method, w.raw()));
}

// Launch a coroutine/sender on the bridge io thread. The official
// exec::start_detached terminates on set_error, so an upon_error log is
// appended (the coroutine bodies below catch everything anyway).
// No stdexec::sender constraint: exec::task completion signatures need an
// environment (get_start_scheduler), which starts_on provides.
template <class S>
void spawn_on_io(S &&sndr) {
  exec::start_detached(
      stdexec::starts_on(*Runtime::instance().io_scheduler(), std::forward<S>(sndr))
      | stdexec::upon_error([](std::exception_ptr ep) noexcept {
          try {
            std::rethrow_exception(ep);
          } catch (const std::exception &e) {
            std::fprintf(stderr, "[wire] detached sender error: %s\n", e.what());
          } catch (...) {
            std::fprintf(stderr, "[wire] detached sender error: unknown\n");
          }
        }));
}

// ─── Per-method coroutines (static functions; MSVC coroutine-lambda workaround) ───

exec::task<void> start_uv_worker_dispatch(std::shared_ptr<Session> session,
                                          std::uint64_t gen, std::uint64_t req,
                                          std::uint32_t method) {
  try {
    auto out = co_await ::foreign_demo::api::start_uv_worker();
    ByteWriter w;
    w.str(out);
    post_ok(session, gen, req, method, w.raw());
  } catch (const std::exception &e) {
    post_err(session, gen, req, method, "start_uv_worker", e.what());
  } catch (...) {
    post_err(session, gen, req, method, "start_uv_worker", "unknown");
  }
  co_return;
}

exec::task<void> call_dart_from_uv_dispatch(
    std::shared_ptr<Session> session, std::uint64_t gen, std::uint64_t req,
    std::uint32_t method,
    dcb::DartFn<std::string(std::string)> callback, std::string input) {
  try {
    auto out = co_await ::foreign_demo::api::call_dart_from_uv(callback, input);
    ByteWriter w;
    w.str(out);
    post_ok(session, gen, req, method, w.raw());
  } catch (const std::exception &e) {
    post_err(session, gen, req, method, "call_dart_from_uv", e.what());
  } catch (...) {
    post_err(session, gen, req, method, "call_dart_from_uv", "unknown");
  }
  co_return;
}

exec::task<void> test_cbridge_invoke_dispatch(
    std::shared_ptr<Session> session, std::uint64_t gen, std::uint64_t req,
    std::uint32_t method,
    dcb::DartFn<std::string(std::string)> callback, std::string input) {
  try {
    auto out = co_await ::foreign_demo::api::test_cbridge_invoke(callback, input);
    ByteWriter w;
    w.str(out);
    post_ok(session, gen, req, method, w.raw());
  } catch (const std::exception &e) {
    post_err(session, gen, req, method, "test_cbridge_invoke", e.what());
  } catch (...) {
    post_err(session, gen, req, method, "test_cbridge_invoke", "unknown");
  }
  co_return;
}

exec::task<void> uv_compute_dispatch(std::shared_ptr<Session> session,
                                     std::uint64_t gen, std::uint64_t req,
                                     std::uint32_t method, std::int32_t n) {
  try {
    auto out = co_await ::foreign_demo::api::uv_compute(n);
    ByteWriter w;
    w.i32(out);
    post_ok(session, gen, req, method, w.raw());
  } catch (const std::exception &e) {
    post_err(session, gen, req, method, "uv_compute", e.what());
  } catch (...) {
    post_err(session, gen, req, method, "uv_compute", "unknown");
  }
  co_return;
}

exec::task<void> test_channel_service_concurrent_dispatch(
    std::shared_ptr<Session> session, std::uint64_t gen, std::uint64_t req,
    std::uint32_t method) {
  try {
    auto out = co_await ::foreign_demo::api::test_channel_service_concurrent();
    ByteWriter w;
    w.str(out);
    post_ok(session, gen, req, method, w.raw());
  } catch (const std::exception &e) {
    post_err(session, gen, req, method, "test_channel_service_concurrent",
             e.what());
  } catch (...) {
    post_err(session, gen, req, method, "test_channel_service_concurrent",
             "unknown");
  }
  co_return;
}

exec::task<void> test_cbridge_async_fail_dispatch(
    std::shared_ptr<Session> session, std::uint64_t gen, std::uint64_t req,
    std::uint32_t method) {
  try {
    auto out = co_await ::foreign_demo::api::test_cbridge_async_fail();
    ByteWriter w;
    w.str(out);
    post_ok(session, gen, req, method, w.raw());
  } catch (const std::exception &e) {
    post_err(session, gen, req, method, "test_cbridge_async_fail", e.what());
  } catch (...) {
    post_err(session, gen, req, method, "test_cbridge_async_fail", "unknown");
  }
  co_return;
}

exec::task<void> test_channel_service_dispatch(std::shared_ptr<Session> session,
                                               std::uint64_t gen,
                                               std::uint64_t req,
                                               std::uint32_t method) {
  try {
    auto out = co_await ::foreign_demo::api::test_channel_service();
    ByteWriter w;
    w.str(out);
    post_ok(session, gen, req, method, w.raw());
  } catch (const std::exception &e) {
    post_err(session, gen, req, method, "test_channel_service", e.what());
  } catch (...) {
    post_err(session, gen, req, method, "test_channel_service", "unknown");
  }
  co_return;
}

exec::task<void> test_cbridge_async_dispatch(std::shared_ptr<Session> session,
                                             std::uint64_t gen,
                                             std::uint64_t req,
                                             std::uint32_t method) {
  try {
    auto out = co_await ::foreign_demo::api::test_cbridge_async();
    ByteWriter w;
    w.str(out);
    post_ok(session, gen, req, method, w.raw());
  } catch (const std::exception &e) {
    post_err(session, gen, req, method, "test_cbridge_async", e.what());
  } catch (...) {
    post_err(session, gen, req, method, "test_cbridge_async", "unknown");
  }
  co_return;
}

exec::task<void> ask_uv_dispatch(std::shared_ptr<Session> session,
                                 std::uint64_t gen, std::uint64_t req,
                                 std::uint32_t method, std::string message) {
  try {
    auto out = co_await ::foreign_demo::api::ask_uv(message);
    ByteWriter w;
    w.str(out);
    post_ok(session, gen, req, method, w.raw());
  } catch (const std::exception &e) {
    post_err(session, gen, req, method, "ask_uv", e.what());
  } catch (...) {
    post_err(session, gen, req, method, "ask_uv", "unknown");
  }
  co_return;
}

exec::task<void> stop_uv_worker_dispatch(std::shared_ptr<Session> session,
                                         std::uint64_t gen, std::uint64_t req,
                                         std::uint32_t method) {
  try {
    auto out = co_await ::foreign_demo::api::stop_uv_worker();
    ByteWriter w;
    w.str(out);
    post_ok(session, gen, req, method, w.raw());
  } catch (const std::exception &e) {
    post_err(session, gen, req, method, "stop_uv_worker", e.what());
  } catch (...) {
    post_err(session, gen, req, method, "stop_uv_worker", "unknown");
  }
  co_return;
}

exec::task<void> test_cbridge_async_cancel_dispatch(
    std::shared_ptr<Session> session, std::uint64_t gen, std::uint64_t req,
    std::uint32_t method) {
  try {
    auto out = co_await ::foreign_demo::api::test_cbridge_async_cancel();
    ByteWriter w;
    w.str(out);
    post_ok(session, gen, req, method, w.raw());
  } catch (const std::exception &e) {
    post_err(session, gen, req, method, "test_cbridge_async_cancel", e.what());
  } catch (...) {
    post_err(session, gen, req, method, "test_cbridge_async_cancel", "unknown");
  }
  co_return;
}

} // namespace

void dispatch_request(std::shared_ptr<Session> session,
                      std::uint64_t session_id, const std::uint8_t *data,
                      std::size_t len) {
  const auto gen = session->generation();
  FrameHeader frame;
  try {
    frame = parse_frame(data, len);
  } catch (const std::exception &e) {
    post_err(session, gen, 0, 0, "dispatch",
             std::string("bad frame: ") + e.what());
    return;
  } catch (...) {
    post_err(session, gen, 0, 0, "dispatch", "bad frame");
    return;
  }

  const auto req = frame.request_id;
  const auto method = frame.method_id;

  try {
    switch (method) {

    case 18716410: {
      ByteReader r(frame.payload.data(), frame.payload.size());

      spawn_on_io(start_uv_worker_dispatch(session, gen, req, method));
      break;
    }

    case 201331851: {
      ByteReader r(frame.payload.data(), frame.payload.size());
      const auto callback = dcb::DartFn<std::string(std::string)>(
          session, gen, r.u64(),
          [](ByteWriter &w, const std::string &a0) { w.str(a0); },
          [](const std::uint8_t *d, std::size_t n) {
            ByteReader r(d, n);
            return r.str();
          });
      const auto input = r.str();
      spawn_on_io(call_dart_from_uv_dispatch(session, gen, req, method,
                                             callback, std::move(input)));
      break;
    }

    case 300843582: {
      ByteReader r(frame.payload.data(), frame.payload.size());
      const auto callback = dcb::DartFn<std::string(std::string)>(
          session, gen, r.u64(),
          [](ByteWriter &w, const std::string &a0) { w.str(a0); },
          [](const std::uint8_t *d, std::size_t n) {
            ByteReader r(d, n);
            return r.str();
          });
      const auto input = r.str();
      spawn_on_io(test_cbridge_invoke_dispatch(session, gen, req, method,
                                               callback, std::move(input)));
      break;
    }

    case 460040803: {
      ByteReader r(frame.payload.data(), frame.payload.size());
      const auto n = r.i32();
      spawn_on_io(uv_compute_dispatch(session, gen, req, method, n));
      break;
    }

    case 595582421: {
      ByteReader r(frame.payload.data(), frame.payload.size());

      spawn_on_io(test_channel_service_concurrent_dispatch(session, gen, req,
                                                           method));
      break;
    }

    case 828415216: {
      ByteReader r(frame.payload.data(), frame.payload.size());
      const auto count = r.i32();
      const auto interval_ms = r.i32();
      auto sink = dcb::StreamSink<std::string>(session, req, gen, method,
                                               [](std::string v) {
                                                 ByteWriter w;
                                                 w.str(v);
                                                 return w.raw();
                                               });
      ::foreign_demo::api::uv_stream(std::move(sink), count, interval_ms);
      break;
    }

    case 1096234678: {
      ByteReader r(frame.payload.data(), frame.payload.size());

      spawn_on_io(test_cbridge_async_fail_dispatch(session, gen, req, method));
      break;
    }

    case 1620642776: {
      ByteReader r(frame.payload.data(), frame.payload.size());

      spawn_on_io(test_channel_service_dispatch(session, gen, req, method));
      break;
    }

    case 1657238865: {
      ByteReader r(frame.payload.data(), frame.payload.size());

      spawn_on_io(test_cbridge_async_dispatch(session, gen, req, method));
      break;
    }

    case 2040169010: {
      ByteReader r(frame.payload.data(), frame.payload.size());
      const auto message = r.str();
      spawn_on_io(ask_uv_dispatch(session, gen, req, method,
                                  std::move(message)));
      break;
    }

    case 2048427889: {
      ByteReader r(frame.payload.data(), frame.payload.size());

      spawn_on_io(stop_uv_worker_dispatch(session, gen, req, method));
      break;
    }

    case 2117983701: {
      ByteReader r(frame.payload.data(), frame.payload.size());

      spawn_on_io(test_cbridge_async_cancel_dispatch(session, gen, req,
                                                     method));
      break;
    }
    default:
      post_err(session, gen, req, method, "dispatch", "unknown method");
      break;
    }
  } catch (const std::exception &e) {
    post_err(session, gen, req, method, "dispatch", e.what());
  } catch (...) {
    post_err(session, gen, req, method, "dispatch", "unknown");
  }
}

std::vector<std::uint8_t> dispatch_sync(std::uint64_t session_id,
                                        const std::uint8_t *data,
                                        std::size_t len) {
  auto frame = parse_frame(data, len);

  throw std::runtime_error("sync: method not sync-capable");
}

} // namespace demo
} // namespace dcb

// Auto-register dispatch at DLL load time.
namespace {
const bool _dcb_registered = [] {
  dcb::set_dispatch(&dcb::demo::dispatch_request, &dcb::demo::dispatch_sync);
  return true;
}();
} // namespace
