name: dcb_foreign_runtime_demo
description: >
  Demo: libuv runtime integrated with dart_cpp_bridge via ForeignExecutor
  C API for non-blocking cross-runtime message passing.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  dart_cpp_bridge:
    path: ../../dart

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
